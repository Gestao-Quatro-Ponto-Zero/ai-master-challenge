# Lead Scorer — variáveis de ambiente
# Copiar para .env.local e preencher

# Opcional — ativa sugestões de ação via LLM (Claude Haiku via OpenRouter)
# Sem esta chave, o app funciona normalmente com fallback determinístico
OPENROUTER_API_KEY=sk-or-v1-...
