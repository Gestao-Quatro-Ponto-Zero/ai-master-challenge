# LOG

Registo cronológico curto de alterações ao repositório (changelog operacional). Para processo, IA e decisões, ver `PROCESS_LOG.md`.

## 2026-03-20

- Governança de submissão: `PROCESS_LOG.md` reestruturado com template reutilizável e secção legado.
- Novos documentos: `docs/SUBMISSION_STRATEGY.md`, `docs/PROCESS_LOG_GUIDE.md`, `docs/CRP_GOVERNANCE.md`, `docs/README_SUBMISSION_SKELETON.md`, `docs/CHALLENGE_CHECKLIST.md` (modelo), `docs/IA_TRACE.md` (placeholder para CRP-S07).
- Nova árvore de evidências: `artifacts/process-log/` com subpastas e `README.md`.
- Novos CRPs de submissão: `CRP-S01` … `CRP-S07` em `crps/`.
- Execução do `CRP-000` refeita: criação de `docs/BASELINE.md` e `docs/REPO_MAP.md` com inventário real do snapshot atual.
- Registro explícito de descompasso entre narrativa do `README.md` e artefatos efetivamente presentes no workspace.
- Execução do `CRP-001`: criação de `docs/DOMAIN.md`, `docs/ARCHITECTURE.md` e `docs/ADR/0001-initial-architecture.md` para restabelecer o trilho documental e de decisão.
- Execução do `CRP-002`: onboarding Python reproduzível com `pyproject.toml`, `scripts/tasks.py`, `docs/SETUP.md` e Quick Start do `README.md` validados por execução real dos comandos.
- Execução do `CRP-003`: contrato de dados em `contracts/repository-data-contract.json`, tipos de domínio Python em `src/domain/models.py`, validação executável em `scripts/validate_data_contract.py` e integração no `build`.
- Execução do `CRP-004`: motor de scoring v0 em `src/scoring/engine.py`, regras externas em `config/scoring-rules.json`, testes `pytest` e documentação em `docs/SCORING.md`.
- Execução do `CRP-005`: API FastAPI para ranking e detalhe em `src/api/app.py`, contrato em `docs/API_CONTRACT.md`, testes HTTP em `tests/test_api_contract.py` e execução manual validada.
- Execução do `CRP-006`: UI shell em `public/` integrada à API (ranking, filtros e detalhe), smoke test de UI e validação manual de assets servidos.
- Execução do `CRP-007`: estratégia de testes em `docs/TEST_STRATEGY.md`, reforço da suíte `pytest` (filtros API + rotas UI) e comando único validado (`python -m pytest -q`).
- Execução do `CRP-008`: quality gates Python com `ruff`, `mypy`, `pip-audit`, geração de SBOM (`artifacts/security/sbom.json`), `pre-commit` e documentação em `docs/QUALITY_GATES.md`.
- Execução do `CRP-009`: workflow de CI para PR em `.github/workflows/ci-pr.yml`, com jobs de qualidade e segurança/SBOM, cache de dependências e seção de CI no README.
- Execução do `CRP-010`: containerização com `Dockerfile`, `docker-compose.yml`, `.dockerignore` e `docs/RUNBOOK.md`, incluindo validação de `docker compose config` e troubleshooting do daemon local.
- Execução do `CRP-011`: observabilidade mínima na API com `x-request-id`, logs JSON estruturados, métricas em `/metrics`, documentação em `docs/OBSERVABILITY.md` e validação manual de rastreabilidade.
- Execução do `CRP-012`: narrativa final de submissão no `README.md`, roteiro de demo em `docs/DEMO_SCRIPT.md`, checklist do desafio preenchido e validação final do pacote (`tasks.py build`).
- Execução do `CRP-D01`: camada raw em Python (`src/raw/reader.py`), inspeção reproduzível (`scripts/inspect_data.py`), snapshot em `artifacts/data-validation/` e documentação de fontes/contrato raw.
- Execução do `CRP-D02`: validação estrutural de qualidade em `src/raw/data_quality.py`, entrypoint `scripts/validate_data_quality.py`, testes dedicados e documentação em `docs/DATA_QUALITY_RULES.md`.
- Execução do `CRP-D03`: camada de normalização semântica em `src/normalization/mapper.py`, mapa versionado `config/normalization-map.json`, ADR-0002 e testes cobrindo `GTXPro -> GTX Pro`.
- Execução do `CRP-D04`: integridade referencial em `src/integrity/referential.py`, entrypoint `scripts/validate_referential_integrity.py`, relatório em `artifacts/data-validation/referential-integrity-report.json` e documentação em `docs/REFERENTIAL_INTEGRITY.md`.
- Execução do `CRP-D05`: modelos canônicos raw/core/gold em `src/domain/models.py`, mapeamento `raw -> core -> gold`, ADR-0003, atualização de `DOMAIN.md`/`DATA_CONTRACT.md` e testes dedicados.
- Execução do `CRP-D06`: `metadata.csv` transformado em dicionário vivo com `scripts/generate_data_dictionary.py`, saída em `docs/DATA_DICTIONARY.md` e cobertura metadata-vs-schema em `artifacts/data-validation/metadata-coverage-report.json`.
- Execução do `CRP-D07`: camada de features em `src/features/engineering.py`, catálogo em `docs/FEATURE_CATALOG.md`, integração do scoring para consumo por `OpportunityFeatureSet` e testes de feature engineering.
- Execução do `CRP-D08`: view models em `src/api/view_models.py`, contrato de payload orientado a vendedor com explainability pronta no backend, documentação em `docs/VIEW_MODELS.md` e testes de serialização/contrato.
- Execução do `CRP-D09`: workflow de CI reforçado com gates de dados/modelagem (`data-quality`, `normalization`, `referential-integrity`, `features`), atualização de `docs/QUALITY_GATES.md`, instrução de badge no `README.md` e validação local espelhando o pipeline.
- Execução do `CRP-D10`: runbook operacional de dados em `docs/RUNBOOK_DATA.md`, script reproduzível `scripts/run_data_runbook.py`, evidências consolidadas em `artifacts/data-validation/runbook-data-evidence.*`, integração no task runner (`runbook-data`) e validação completa por execução real.
- Execução do `CRP-API-01`: contrato HTTP UI/API canônico definido com endpoints de oportunidades e dashboard, modelos Pydantic em `src/api/contracts.py`, compatibilidade mantida com `/api/ranking`, documentação em `docs/API_CONTRACT_UI.md` e ADR-0004 com decisões de versionamento.
- Execução do `CRP-API-02`: cliente HTTP Python centralizado com `httpx` em `src/infrastructure/http/api_client.py`, mapeamento de erros de integração, configuração via ambiente (`.env.example`), documentação em `docs/API_CLIENT.md` e testes dedicados do cliente.
- Execução do `CRP-API-03`: DTOs wire e mappers explícitos em `src/infrastructure/http/` para separar payload JSON do contrato tipado, integração dos mappers no `src/api/app.py`, testes de borda em `tests/test_http_mappers.py` e documentação em `docs/API_DTO_MAPPERS.md`.
- Execução do `CRP-API-04`: `ApiOpportunityRepository` implementado em `src/infrastructure/repositories/` consumindo o cliente HTTP, com mapeamento de erro de integração para erro de domínio de repositório, testes de sucesso/404/erro genérico e documentação em `docs/API_REPOSITORY.md`.
- Execução do `CRP-API-05`: alternância mock/API centralizada por flag (`LEAD_SCORER_REPOSITORY_MODE`) em `repository_factory.py`, mock repository dedicado para modo demo, documentação em `docs/RUNTIME_MODES.md`, `.env.example` atualizado e testes de seleção de modo.
- Execução do `CRP-API-06`: filtros reais de listagem plugados com serialização estável de query params (`filter_params.py`), suporte server-side para `q`, `sort_by` e `sort_order` em `/api/opportunities`, atualização do contrato UI/API e documentação do modelo de execução em `docs/FILTER_EXECUTION_MODEL.md`.
- Execução do `CRP-API-07`: estados de erro e observabilidade da integração refinados com mapeamento explícito no `ApiClient` (`404`, `422`, `5xx`, timeout), propagação de `x-request-id` até exceções, novas métricas por status no backend e documentação operacional em `docs/OBSERVABILITY_INTEGRATION.md`.
- Execução do `CRP-API-08`: testes de contrato com simulação HTTP em Python via `pytest-httpx` (`tests/test_api_client_contract_http.py`), atualização de dependências de dev/audit e documentação operacional em `docs/API_TESTING.md` com definição de quebra de contrato e fluxo de atualização de fixtures.
- Execução do `CRP-API-09`: remoção de acoplamento residual de mock no caminho API com factory lazy, default operacional em modo `api`, desexportação de mock no `__init__` do pacote e relatório de limpeza em `docs/MOCK_DECOMMISSIONING.md`.
- Execução do `CRP-API-10`: criação do runbook de integração UI/backend em `docs/RUNBOOK_UI_API_INTEGRATION.md`, com variáveis, fluxo de subida, checklist smoke E2E, troubleshooting (porta, 422, timeout, CORS/correlation id) e alinhamento de links em `README.md` e `docs/SETUP.md`.
- Execução do `CRP-UI-01`: baseline e governança da trilha UI consolidados com `docs/ARCHITECTURE_UI.md` e `docs/REPO_MAP_UI.md`, além de atualização do `README.md` para incorporar os novos artefatos de frontend no mapa documental.
- Execução do `CRP-UI-02`: alinhamento do contrato frontend ao dataset real com `docs/DATA_CONTRACT_FRONTEND.md`, ADR `0005-frontend-dataset-contract-alignment.md` e reforço de teste de contrato para restringir `statuses` da API ao conjunto esperado (`Open|Won|Lost`).
- Execução do `CRP-UI-03`: separação de contrato de repositório e implementações na UI shell (`public/application/contracts` e `public/infrastructure/repositories`), adoção de factory para DI em `public/app.js`, atualização de smoke tests e documentação em `docs/REPOSITORY_STRATEGY.md`.
- Execução do `CRP-UI-04`: isolamento de mocks como fixtures de demo em `public/infrastructure/mocks/fixtures/`, refatoração do `MockOpportunityRepository` para consumir fixtures organizadas, documentação em `docs/MOCK_STRATEGY.md` e reforço do smoke test para impedir vazamento de mocks para a apresentação.
- Execução do `CRP-UI-05`: endurecimento dos quality gates de UI para o snapshot atual (JS sem pipeline TypeScript) com script dedicado `scripts/validate_ui_quality.py`, novos comandos `ui-quality`/`ui-ci-check` no task runner, integração no `build` e documentação em `docs/QUALITY_GATES_UI.md`.
- Execução do `CRP-UI-06`: refinamento da camada de dados do dashboard com query keys padronizadas (`public/shared/query/query-keys.js`), módulo de orquestração com retry/cancelamento (`public/presentation/hooks/use-dashboard-data.js`), adaptação da UI para consumo via hook e documentação em `docs/QUERY_STRATEGY.md`.
- Execução do `CRP-UI-07`: melhoria da UX operacional com tabela navegável por teclado, persistência de seleção após filtros, estados explícitos de erro/loading/not found no drawer, ações de retry/close e documentação de decisões em `docs/UX_DECISIONS.md`.
- Execução do `CRP-UI-08`: limpeza de traços de template e consolidação de branding para "Focus Score Cockpit" em `public/index.html` (title/meta/header), ajustes documentais em `README.md`/`docs/ARCHITECTURE_UI.md` e validação dos smoke tests da UI.
- Execução do `CRP-UI-09`: ampliação da cobertura de testes do frontend com suíte dedicada `tests/test_ui_front_coverage.py`, novo comando `ui-tests` no task runner, integração da cobertura no `ui-quality` e documentação específica em `docs/TEST_STRATEGY_UI.md`.
- Execução do `CRP-UI-10`: CI dedicada do frontend em `.github/workflows/frontend-ci.yml` (gates `ui-tests` e `ui-quality`), documentação em `docs/CI_FRONTEND.md` e atualização de README/quality gates com referência de pipeline e badge.
- Execução do `CRP-S01`: template do `PROCESS_LOG.md` harmonizado com o guia (`Decomposição / subpassos` e `Julgamento / correção humana`) para padronização auditável.
- Execução do `CRP-S02`: estrutura de evidências `artifacts/process-log/` garantida por subpastas versionadas com `.gitkeep` e README reforçado com convenção de paths relativos.
- Execução do `CRP-S03`: README de submissão revisado a partir do esqueleto oficial, com reforço de critérios verificáveis (desafio explícito, processo auditável e ligação à estratégia de PR/evidências).
- Execução do `CRP-S04`: checklist do desafio validado contra evidências reais do repositório, com estados `ok/parcial` honestos e gap explícito para execução remota de CI.
- Execução do `CRP-S05`: revisão final de submissão preparada para PR com reforço de `.github/PULL_REQUEST_TEMPLATE.md` e documentação equivalente de revisão em `artifacts/process-log/decision-notes/crp-s05-pr-equivalente-sem-remoto.md`.
- Execução do `CRP-S06`: auditoria anti-genérico aplicada em docs públicas, com nota `antes -> depois` em `artifacts/process-log/decision-notes/crp-s06-auditoria-output-generico-vs-verificavel.md`.
- Execução do `CRP-S07`: consolidado de prompts/respostas/correções humanas publicado em `docs/IA_TRACE.md` e referenciado na estratégia de submissão.
- Execução do `CRP-REAL-01` (gap-closure pack): modo padrão `real_dataset` para o serving HTTP (`LEAD_SCORER_DATA_SOURCE_MODE`), carregamento dos CSVs oficiais em `src/api/dataset_loader.py`, integração em `src/api/app.py`, testes `tests/test_real_dataset_serving.py` + `tests/conftest.py`, documentação `docs/RUNTIME_DATA_FLOW.md`, README (secção Data Source), `.env.example`, `docker-compose.yml`, `docs/RUNTIME_MODES.md`, `docs/MOCK_DECOMMISSIONING.md`, `docs/SETUP.md` e evidências em `artifacts/process-log/`.
- Execução do `CRP-REAL-02` (gap-closure pack): pipeline de serving em `src/serving/` (5 CSVs, joins `products`/`metadata`, validação de FKs, modelo `ServingOpportunity`, cache por mtime), `dataset_loader` delegando no pipeline, `docs/SERVING_REAL_DATA.md`, atualização de `docs/RUNTIME_DATA_FLOW.md` e README, testes `tests/test_serving_pipeline_integration.py`, evidência JSON de detalhe real em `artifacts/process-log/test-runs/crp-real-02-detail-1C1I7A6R.json`.
- Execução do `CRP-REAL-03` (gap-closure pack): domínio runtime alinhado ao challenge com `deal_stage` (`Prospecting`/`Engaging`/`Won`/`Lost`), remoção de `Open`/`ui_status` na superfície principal, módulo `src/domain/deal_stage.py`, ajustes em `src/api/app.py`, contratos, serving, cliente HTTP/repos mock, `data/demo-opportunities.json`, UI (`public/`), testes (`tests/test_deal_stage.py` + atualizações), `docs/DOMAIN_ALIGNMENT.md` e documentação de contrato; evidência em `artifacts/process-log/decision-notes/crp-real-03-domain-alignment.md`.
- Execução do `CRP-REAL-04` (gap-closure pack): scoring enriquecido com features do dataset (`feature_set_from_payload`, `config/scoring-rules.json` v2), serving row com `account_revenue`/`account_employees`/`close_date`/`team_regional_office`/`product_sales_price`, refactor `src/scoring/engine.py`, `docs/SCORING_V2.md`, testes `tests/test_scoring_v2_features.py`, evidência `artifacts/process-log/decision-notes/crp-real-04-scoring-evidence.md`, atualizações em `docs/SCORING.md`, `LOG.md`, `PROCESS_LOG.md`, `README.md`.
- Execução do `CRP-REAL-05` (gap-closure pack): UI alinhada ao dataset real — colunas explícitas (conta, produto, vendedor, escritório, valor fecho), KPIs via `getDashboardKpis`, detalhe estruturado, copy B2B no header; API estende `OpportunityListItemResponse`/`OpportunityDetailResponse` + `_real_data_fields_from_row`; `docs/UI_REAL_DATA_ALIGNMENT.md`, evidência `artifacts/process-log/decision-notes/crp-real-05-ui-real-data.md`, atualizações em `docs/API_CONTRACT_UI.md`, `docs/DEMO_SCRIPT.md`, `PROCESS_LOG.md`, `README.md`.
- Execução do `CRP-REAL-06` (gap-closure pack): isolamento de ruído — pastas `focus-score-*`, `prompts/` e ferramenta de concat movidas para `legacy/`; `legacy/README.md`, `docs/REPO_SHAPE.md`, evidência `artifacts/process-log/decision-notes/crp-real-06-repo-cleanup.md`; `.gitignore` e `scripts/tasks.py` (lint MD) ajustados; atualizações em `README.md`, `00-START-HERE.md`, `docs/REPO_MAP.md`, `ARCHITECTURE`, `BASELINE`, `REPO_MAP_UI`, `LOG.md`, `PROCESS_LOG.md`.
- Execução do `CRP-REAL-07` (gap-closure pack): `README.md` reescrito como **submissão final** (Executive Summary, Abordagem, Resultado, Recomendações, Limitações, Setup, Demo); redução de ruído metodológico na primeira leitura; novo `docs/README_DECISIONS.md`; evidência e checklist em `artifacts/process-log/decision-notes/crp-real-07-readme-submission.md`; `LOG.md` e `PROCESS_LOG.md` atualizados.
- Execução do `CRP-REAL-08` (gap-closure pack): testes do **fluxo principal** com dataset real — `tests/test_real_dataset_main_flow.py` (filtros ranking/opportunities, pesquisa `q`, explainability em detalhe, KPIs); `docs/TEST_STRATEGY_REAL_DATA.md`; evidência `artifacts/process-log/decision-notes/crp-real-08-real-data-tests.md`; atualizações em `docs/TEST_STRATEGY.md`, `README.md`, `LOG.md`, `PROCESS_LOG.md`.
- Execução do `CRP-REAL-09` (gap-closure pack): demo e evidências alinhadas ao **fluxo real** — `docs/DEMO_SCRIPT.md` revisto (sem `OPP-001` como exemplo principal); `scripts/export_real_flow_evidence.py` + task `export-real-flow-evidence`; JSON `crp-real-09-*.json` e refresh de `crp-real-01-sample-ranking-real.json` / `crp-real-02-detail-1C1I7A6R.json`; guia `artifacts/process-log/ui-captures/REAL-09-SCREENSHOT-GUIDE.md`; nota `artifacts/process-log/decision-notes/crp-real-09-demo-real-flow.md`; `README.md`, `LOG.md`, `PROCESS_LOG.md`.
- Execução do `CRP-REAL-10` (gap-closure pack): **auditoria final** contra requisitos Challenge 003 — `docs/FINAL_AUDIT_CHALLENGE_003.md` (matriz, pendências bloqueantes/não bloqueantes, parecer **SUBMETER**); evidência `artifacts/process-log/decision-notes/crp-real-10-final-audit.md`; `docs/CHALLENGE_CHECKLIST.md` (revisão), `README.md`, `LOG.md`, `PROCESS_LOG.md`.
- Execução da trilha **CRP-UX-01 … CRP-UX-10** (`legacy/focus-score-ux-competition-pack/`): cockpit UX (HTML/CSS/JS), detalhe em cartões sem JSON cru, filtros com limpar/contagem/`q`/`priority_band`, API `list_opportunities` com `_row_matches_search_q` + `priority_band`, testes `tests/test_ui_competition_pack.py`, `docs/UX_NOMENCLATURE.md`, guia de capturas `artifacts/process-log/ui-captures/UX-COMPETITION-FINAL-GUIDE.md`, nota `artifacts/process-log/decision-notes/crp-ux-competition-pack.md`, atualizações em `docs/UX_DECISIONS.md`, `docs/DEMO_SCRIPT.md`, `README.md`, `PROCESS_LOG.md`.
- Execução da trilha **CRP-CBX-01 … CRP-CBX-07** (`focus-score-combobox-crps-pack/`): `filter-options` com `regional_offices` + `regions` espelho; UI com `<select>` para escritório/estágio e **combobox** para gestor (3+ letras); `manager-combobox.js`, estilos em `styles.css`; testes e docs (`API_CONTRACT_UI`, `UX_DECISIONS`); evidência `artifacts/process-log/decision-notes/crp-cbx-combobox-filters.md`; `LOG.md` / `PROCESS_LOG.md`.
- Execução do **CRP-CBX-08**: combobox de gestor reforçado — `filterManagersByQuery` (prefixo → contém), estados **A carregar gestores…** / helper / erro API, `aria-busy` + `aria-activedescendant`, fecho ao clicar fora, `destroy` ao re-init; guia `artifacts/process-log/ui-captures/CBX-08-SCREENSHOT-GUIDE.md`, resumo `docs/CBX-08_MANAGER_COMBOBOX.md`, nota `artifacts/process-log/decision-notes/crp-cbx-08-manager-combobox.md`, `focus-score-combobox-crps-pack/crps/CRP-CBX-08.md`, testes em `test_ui_competition_pack.py`, `PROCESS_LOG` / `LOG` / `UX_DECISIONS`.

## 2026-03-21

- **Faxina final (pacote `submissions/leopoldo-lima/` no repo `ai-master-challenge`):** removidas duplicatas `solution/docs/`, `solution/crps/`, `solution/PROCESS_LOG.md`; `scripts/tasks.py` valida caminhos na pasta da submissão (pai de `solution/`); `solution/artifacts/process-log/` só para saídas de scripts; evidências entregues em `process-log/`. Referências `submission/README.md` corrigidas para `submissions/leopoldo-lima/README.md`. README de submissão reforçado (navegação, challenge 003, LinkedIn). `generate_data_dictionary.py` / `test_data_dictionary.py` passam a usar `docs/` na pasta da submissão.
- Refino **CRP-CBX-08**: `wireManagerCombobox` com `AbortController` + limpeza de timers em `destroy` (evita listeners duplicados em re-init); **Esc** fecha lista e esconde «Nenhum gestor encontrado»; script `scripts/capture_cbx08_screenshots.py` (Playwright) gera PNG em `artifacts/process-log/ui-captures/cbx-08/`; guias e notas de processo atualizados com caminhos das evidências.
- **CRP-FIN-01** (final tuning): combobox de gestor com **lista completa ao focar**, filtro incremental (`listAllOrFilterManagers`), opção «Todos os gestores», textos e testes de UI atualizados; nota `artifacts/process-log/decision-notes/crp-fin-01-combobox-lista-total.md`.
- **CRP-FIN-02**: registo de regras de busca, empty states e checklist a11y em `crp-fin-02-combobox-a11y.md`; `docs/UX_DECISIONS.md` alinhado.
- **CRP-FIN-03**: recalibração `deal_stage_weights` para priorizar pipeline aberto vs. `Won` dominante; nota `crp-fin-03-open-pipeline-baseline.md` e secção em `docs/SCORING_V2.md`.
- **CRP-FIN-04**: reforço de `pipeline_age`, penalidade de engage ausente, `open_operational` + função no motor; nota `crp-fin-04-open-features.md`.
- **CRP-FIN-05**: catálogo `actions_by_context` e seleção por estágio/banda/stale + hash do id; nota `crp-fin-05-next-actions.md`.
- **CRP-FIN-06**: camada `explanation_narrative` para texto comercial no detalhe; nota `crp-fin-06-humanize-explanations.md`.
- **CRP-FIN-07**: matriz `docs/EXPLANATION_NARRATIVE.md` + mais uma variante `Won`; nota `crp-fin-07-explanation-diversity.md`.
- **CRP-FIN-08**: `focus-score-combobox-crps-pack` e `focus-score-final-tuning-crps-pack` movidos para `legacy/`; `legacy/PACKS_ARCHIVE.md`; nota `crp-fin-08-packs-legacy.md`.
- **CRP-FIN-09**: `docs/RUNTIME_DEFAULTS.md` + cabeçalho em `.env.example` (padrão real vs demo/mock explícitos); nota `crp-fin-09-runtime-defaults.md`.
- **CRP-FIN-10**: `README.md` com framing explícito de submissão Challenge 003 + link a runtime padrão; nota `crp-fin-10-readme-submission.md`.
- **CRP-FIN-11**: checklist `docs/FIN-11_AUDIT_CHECKLIST.md` + nota `crp-fin-11-audit.md`.
- **CRP-FIN-12**: `docs/SUBMISSION_PACKAGE.md` + nota `crp-fin-12-submission-package.md` (fecho pacote PR).
- **CRP-DEL-01** (entrega): `docs/DELIVERY_INVENTORY.md` + versionamento do pack `focus-score-delivery-submission-crps-pack/`; `PROCESS_LOG.md` / `LOG.md` atualizados.
- **CRP-DOC-01**: `submissions/leopoldo-lima/README.md` (README de submissão alinhado ao template); `PROCESS_LOG.md` / `LOG.md` atualizados.
- **CRP-DOC-02**: `docs/DELIVERY_NOTES.md` + link no `submissions/leopoldo-lima/README.md`; referência metodológica sóbria; logs atualizados.
- **CRP-DOC-03**: `docs/EVIDENCE_INDEX.md` + link no `submissions/leopoldo-lima/README.md`; logs atualizados.
- **CRP-SUB-01**: `docs/SUBMISSION_GUIDE_AUDIT.md`; `docs/CHALLENGE_CHECKLIST.md` (nota de cruzamento); logs atualizados.
- **CRP-SUB-02**: `docs/SUBMISSION_TEMPLATE_AUDIT.md`; `submissions/leopoldo-lima/README.md` (Submissão via PR); logs atualizados.
- **CRP-SUB-03**: `docs/PR_HANDOFF.md`; `submissions/leopoldo-lima/README.md` (link ao handoff); logs atualizados.
- **CRP-VID-01**: `docs/VIDEO_SCRIPT.md` + `artifacts/process-log/decision-notes/video-script.md`; logs atualizados.
- **CRP-VID-02**: `scripts/record_demo_chromium.py` + `docs/VIDEO_RUNBOOK.md`; logs atualizados.
- **CRP-VID-03**: gravação `demo-cockpit.webm` + PNG em `artifacts/process-log/screen-recordings/`; correção seletor no script; índices e README de submissão atualizados; logs atualizados.
- **Refino demo vídeo**: `record_demo_chromium.py` com `--pace slow|fast`, filtros (escritório, estágio, prioridade), pesquisa com texto real da API e digitação visível no combobox de gestores; WebM + 7 PNG regenerados; runbook e índices atualizados.
- **Demo vídeo (scroll + aplicar)**: após cada filtro o script clica **Aplicar filtros**, espera o ranking e rola até à tabela/contagem; rolagem smooth para KPIs, filtros, ranking e detalhe; fallback **Limpar filtros** se o ranking ficar vazio; `VIDEO_RUNBOOK` e artefatos regenerados.
- **CRP-SUB-08**: CRPs curados em `crps/executed/` (foundation/data/ui/product-tuning/submission); índice `indexes/crp-index.csv`; lista plana arquivada em `archive/superseded/crps-root-flat-pre-SUB-08/`; pacote `focus-score-curated-crps` movido para `archive/superseded/focus-score-curated-crps/`; narrativa GPT-5.4 / CRP / Cursor / Lovable / Vibecoding Industrial em `README.md`, `docs/IA_TRACE.md`, `docs/SUBMISSION_STRATEGY.md`; evidência `artifacts/process-log/decision-notes/crp-sub-08-curation.md`; `PROCESS_LOG.md` atualizado.
