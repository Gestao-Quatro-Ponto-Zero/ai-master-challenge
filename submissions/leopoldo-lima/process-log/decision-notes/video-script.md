# Cópia de trabalho — roteiro vídeo (CRP-VID-01)

Espelho operacional de [`docs/VIDEO_SCRIPT.md`](../../docs/VIDEO_SCRIPT.md) para rastreio no process log.

**Ordem de cenas (Chromium):**

1. Dashboard com KPIs carregados (`#kpi-strip` visível).
2. Filtro de estágio aplicado (`#filter-deal-stage`).
3. Clique na primeira linha do ranking (`#ranking-list tr`).
4. Painel de detalhe com «Porque este score?» (`.detail-card`).
5. Combobox gestor: foco + digitação (`#manager-search`, `#manager-listbox`).

**Gravação técnica:** `solution/scripts/record_demo_chromium.py` + `docs/VIDEO_RUNBOOK.md` (executar a partir da pasta `solution/` quando aplicável).
