[build]
builder = "dockerfile"
# dockerfilePath é relativo ao rootDirectory configurado no Railway (submissions/bubex)
dockerfilePath = "apps/portal/Dockerfile"

[deploy]
healthcheckPath = "/"
restartPolicyType = "on_failure"
