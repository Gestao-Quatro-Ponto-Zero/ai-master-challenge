import { EmptyState } from "@/components/ui";

export default function NotFound() {
  return <EmptyState title="Unsupported view" message="Use the primary navigation to return to a governed dashboard section." />;
}
