import { EmptyState } from "@/components/ui";
export default function NotFound() { return <EmptyState title="Visualização não encontrada" message="Use a navegação principal para voltar a uma seção governada do painel." />; }
