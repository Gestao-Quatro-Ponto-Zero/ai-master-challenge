# Dev-log — Passo 1.0: Scaffold do notebook de EDA adversarial

**Bloco 1, passo 1.0.** Registro de desenvolvimento assistido por IA.

| Papel           | Quem                                | Função neste passo                       |
| --------------- | ----------------------------------- | ---------------------------------------- |
| Arquiteto       | Alan Gattiboni                      | Define objetivo e valida a entrega       |
| ArquiEngenheiro | Claude                              | Escreve a instrução parametrizada abaixo |
| Executor        | Codinho (agente de código, VS Code) | Implementa e reporta                     |

---

## Instrução (Claude para Codinho)

**Objetivo:** criar o scaffold do notebook de EDA adversarial do Bloco 1. Escopo
deste passo: estrutura de seções e carga dos dois datasets.

**Arquivo a criar:** `solution/01_eda_adversarial.ipynb`

**Dados de entrada** (já presentes em `solution/datasets/`):

| Dataset | Arquivo                                 | Linhas esperadas |
| ------- | --------------------------------------- | ---------------- |
| D1      | `customer_support_tickets.csv`          | 8.469            |
| D2      | `all_tickets_processed_improved_v3.csv` | 47.837           |

O Dataset 1 contém descrições que ocupam várias linhas físicas dentro do
arquivo. Um parsing correto colapsa isso em 8.469 tickets. Se a contagem vier em
outro valor (por exemplo ~29.808), trate como bug de parsing e reporte o número
real.

**Requisitos inegociáveis:**

1. Uma função de carga parametrizada. Recebe o nome do arquivo, resolve o
   caminho a partir de `solution/datasets/` de forma relativa e reproduzível
   (sem caminho absoluto de máquina), retorna o DataFrame. A mesma função
   carrega os dois datasets.
2. Leitura em UTF-8, sem warning de tipo misto.
3. Uma célula de sanity check que imprime o `df.shape` dos dois e confirma 8.469
   e 47.837. Se algum valor não bater, imprime o número real com um aviso
   destacado.
4. Esqueleto em markdown das seções 1.1 a 1.6, cada uma com título e uma linha
   do que investiga. Sem conteúdo de análise.
5. O notebook roda inteiro por Restart & Run All sem erro.

**Liberdade sua:** implementação da função, organização das células, detalhes
dos parâmetros de leitura.

**Não faça:** análise, gráfico, transformação, `git add`, commit, push.

**Formato do retorno:** markdown inline, para colar na seção abaixo. Inclua:

1. Caminho do arquivo criado.
2. Assinatura da função de carga e o que ela faz, em uma linha.
3. Saída real do sanity check: `shape` dos dois DataFrames.
4. Lista de colunas de cada dataset.
5. Warnings ou surpresas no carregamento. Se não houve, escreva "sem warnings".
6. Confirmação de Restart & Run All sem erro.

---

## Retorno

1. Arquivo criado: solution/01_eda_adversarial.ipynb

2. Função de carga: load_dataset(filename: str) -> pd.DataFrame — resolve
   solution/datasets/ de forma relativa (tenta datasets/ a partir do diretório
   do notebook e, como fallback, sobe a árvore procurando solution/datasets/,
   sem caminho absoluto de máquina) e lê o CSV em UTF-8 com low_memory=False. A
   mesma função carrega D1 e D2.

3. Saída real do sanity check:

D1: shape = (8469, 17) [OK] D2: shape = (47837, 2) [OK] Contagens batem com o
esperado (8.469 / 47.837) — o parsing colapsou corretamente as descrições
multilinha do D1.

4. Colunas:

D1 (17): Ticket ID, Customer Name, Customer Email, Customer Age, Customer
Gender, Product Purchased, Date of Purchase, Ticket Type, Ticket Subject, Ticket
Description, Ticket Status, Resolution, Ticket Priority, Ticket Channel, First
Response Time, Time to Resolution, Customer Satisfaction Rating D2 (2):
Document, Topic_group 5. Warnings ou surpresas: sem warnings. Nenhum
DtypeWarning de tipo misto (low_memory=False lê o arquivo inteiro antes de
inferir tipos) e nenhum problema de encoding em UTF-8.

6. Restart & Run All: confirmado — executado ponta a ponta via jupyter nbconvert
   --to notebook --execute --inplace, que reinicia um kernel limpo e roda todas
   as células em ordem. Zero erros; outputs salvos no notebook.
